# PostgreSQL connection string (Neon recommended for local + production)
# Local Docker: postgresql://cafe:cafe@localhost:5432/cafe_pos?schema=public
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
